"use client";
import ComponentsPreviewShowcase from "./ComponentsPreviewShowcase";

export const ComponentsPage = () => {
  // if (isLoading) return <Loader />;
  return (
    <div>
      <ComponentsPreviewShowcase />
    </div>
  );
};
